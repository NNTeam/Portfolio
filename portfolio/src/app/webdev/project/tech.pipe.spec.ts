/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TechPipe } from './tech.pipe';

describe('Pipe: Tech', () => {
  it('create an instance', () => {
    let pipe = new TechPipe();
    expect(pipe).toBeTruthy();
  });
});
